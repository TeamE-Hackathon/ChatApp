name: deploy prod
on:
  push:
    tags:
      - "*"
jobs:
  api-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: push ECR
        uses: ./infra/build.sh

  front-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: sync S3
        uses: ./infra/ship.sh
  # front-ship:
  # front-ship:
